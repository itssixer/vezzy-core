# Security

This repository is a static UI and an in-browser preview loop. It does not host accounts or execute untrusted code on a server.

If you find a bug in the demo sandbox (for example a way for `srcdoc` preview code to reach the parent origin), open a GitHub issue with reproduction steps and no secrets.

Do not report hosted-product account issues against this repository. Those systems are not shipped here.
